CREATE TABLE safegraph_patterns (placekey text,
 safegraph_place_id text,
 location_name text,
 street_address text,
 city text,
 region text,
 postal_code text,
 safegraph_brand_ids text,
 brands text,
 date_range_start text,
 date_range_end text,
 raw_visit_counts integer,
 raw_visitor_counts integer,
 visits_by_day text,
 poi_cbg text,
 visitor_home_cbgs text,
 visitor_daytime_cbgs text,
 visitor_work_cbgs text,
 visitor_country_of_origin text,
 distance_from_home text,
 median_dwell numeric,
 bucketed_dwell_times text,
 related_same_day_brand text,
 related_same_month_brand text,
 popularity_by_hour text,
 popularity_by_day text,
 device_type text);
